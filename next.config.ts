import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // basePath and assetPrefix removed for Vercel deployment
  // Only needed for EC2 deployment under /finboard path
};

export default nextConfig;
